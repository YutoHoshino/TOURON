= render "homes/load"
.talk-main
  = render "homes/header"
  = render "homes/nav"

  .all-box
    = render "left-sidebar"
    = render "modal"
    .message-box
      = render "talks"
      - if @stasu.status.present?
        = render "end_footer"
      - else
        = render "footer"
    = render "right-sidebar"